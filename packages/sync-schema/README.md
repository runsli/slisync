# @slisync/sync-schema

Shared types and contracts for the **Slisync** protocol and Memory Graph.

[English](../../README.md) · [中文](../../README.zh-CN.md) · [docs/en/VISION.md](../../docs/en/VISION.md) · [packages/README.md](../README.md)

```bash
npm install @slisync/sync-schema
```

Exports include: `GraphOp`, `MemoryNode` / `MemoryEdge`, `TraverseQuery`, `AgentGraphPolicy`, `MemoryScope`, `PresenceMember`, `SYNC_PROTOCOL_VERSION`.
